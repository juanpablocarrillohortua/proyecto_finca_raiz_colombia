# Runtime dependencies for the FincaRaiz pipeline scraper.
# Install with:  pip install -r requirements.txt
# Lint tooling lives separately in requirements/dev.txt

# brotli extra: the site serves br-encoded HTML. Without a brotli codec
# httpx cannot decode it and every selector silently misses.
httpx[http2,brotli]>=0.27
selectolax>=0.3.21
pydantic>=2.7
pydantic-settings>=2.3
tenacity>=8.3
structlog>=24.1
typer>=0.12
PyYAML>=6.0

# Testing (stage 3/4 fixtures, no network)
pytest>=8.2
