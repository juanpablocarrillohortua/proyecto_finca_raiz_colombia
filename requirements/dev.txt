# Linting and formatting tooling. Install with: make install-dev
#
# ruff replaces pycodestyle, pyflakes, isort and pep8-naming, and lints
# Jupyter notebooks cell by cell. (.ipynb support is on by default from 0.6.)
#
# Pinned, not floating: the CI gate in .github/workflows/quality.yml installs
# from this file, and a linter that upgrades itself turns the build red with
# nobody having touched the code. Same version as requirements.txt. Bump it
# deliberately, and run `make quality` locally when you do.
ruff==0.16.1
